Feature: Login page
  The Login page should work as expected

Scenario: page loads successfully
  Given a user opens the browser
  When a user navigates to the Login Page
  Then the user sees the page rendered